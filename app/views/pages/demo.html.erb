<nav class="navbar navbar-default">
      <div class="navbar-header">
        <a class="navbar-brand">Xvisor</a>
      </div>
      <ul class="nav navbar-nav navbar-right">
      	<li><%= link_to("Unreported Scans", images_path,method: :get) %> </li>
        <li><%= link_to("Reported Scans", images_check_path, method: :get) %></li>
        <li><%= link_to("API Demo", pages_demo_path, method: :get) %></li>
        <li><%= link_to("Sign Out", destroy_user_session_path, method: :delete) %> </li>

      </ul>
    </nav>
<h1 style="margin-top:1em;"">API Demo</h1>
<div style="margin-top:9em; margin-bottom: 0" class="container-fluid">
	<div class="row" style="background-color: #F1F0F0">
		<div class="col-sm-6" style="border-right: solid; margin-top: 5em;">
			<form>
			    <input type="text" name="Image URL" id="url_box">
			    <button onclick="sendRequest(event); test(event)">Send Request</button>
			    <br>
			    <br>
			    <input style="margin: auto" type="file" onchange="previewFile()"><br>
			    <div style="height:300px; border: solid; width: 50%">
			    	<img id="input_img" src="" alt="Image preview..." style="height:300px;">
			    </div>
			</form>
		</div>
	<br>
		<div class="col-sm-6">
			<div style="border-bottom:solid; height: 100px; width: 50%;"><p style="font-size: 25px;" id="score"></p></div>
			<br>
			<br>
			<div style="height:300px; border: solid; width: 50%">
			    <img id="output_img" src="" alt="Image preview..." style="height:300px;">
			</div>
			<br>
			<div style ="display:none;" id="normal"><h2>No further investigation recommended </h2></div>
			<div style ="display:none;" id="abnormal"><h2>Further investigation recommended</h2></div>
		</div>
	</div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>




<script>
   function previewFile(){
       var preview = document.querySelector('img'); //selects the query named img
       var file    = document.querySelector('input[type=file]').files[0]; //sames as here
       var reader  = new FileReader();

       reader.onloadend = function () {
           preview.src = reader.result;
       }

       if (file) {
           reader.readAsDataURL(file); //reads the data as a URL
       } else {
           preview.src = "";
       }
  }

  previewFile();  //calls the function named previewFile()
    
    function sendRequest(e){
    	e.preventDefault();
        $.get(
            "http://104.196.225.45:5000/scan",
            {
                url: $("#url_box").val()
            }).done(function(data){
                var s = data.score;
    			$("#input_img").attr('src', data.original); 
    			$("#score").html("The abnormality index of the given image is " + s + );
    			if(s < 0.5){
    				$("#JSON").html("No obvious pulmonary mass lesion or consolidation is seen.<br> Cardiac outline is normal.");
    				$("#normal").show();
    			} else{
    				$("#output_img").attr('src', data.heatmap);
    				$("#abnormal").show();
    			}
            })
       
    }
/*
    function test(e){
    	$("#JSON").innerHTML = "hullo";

    	document.getElementById("JSON").innerHTML = "HULLO";

    	
    	$("#JSON").html("The abnormality index of the given image is: " + data.score);
    	if(data.score < 0.5){
    		#("#Format").html("Th")
    	}
    	
    }
*/
    /*

    $("#url_box").keyup(function(){
    	var image_url = $("#url_box").val();
    	$("#input_img").attr('src', image_url); 
    })
	*/

  </script>
